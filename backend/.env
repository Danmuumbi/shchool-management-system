# Central Postgres admin (used to create tenant DBs; must have CREATE DATABASE privileges)
CENTRAL_DB_URI=postgresql://postgres:%40Mmuuo2015@localhost:5432/evoltechs_central


# SMTP (Gmail) - use App Passwords for Gmail or your SMTP provider
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=muuomuumbi@gmail.com
SMTP_PASS=irqb yuvy ivoc veyi

# Flask
FLASK_ENV=development
SECRET_KEY=replace_with_a_random_secret
JWT_SECRET_KEY=evoltechs_secret_2025

# Application host (used for building subdomain urls in emails)
APP_DOMAIN=localhost    # for production: evoltechs.top
APP_PORT=5000
